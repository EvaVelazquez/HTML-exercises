const array = [1, 3, 4, 5, 10, 16]
array.splice(1, 0, 2)
console.log (array);

const map = new Map();
const initialPlant = {id:1, name:"Pikachu"};
map.set(initialPlant.id, initialPlant);
const initialCharmander = {id:4, name:"Charmander"}
